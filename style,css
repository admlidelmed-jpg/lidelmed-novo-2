body {
  margin: 0;
  font-family: Arial, sans-serif;
  background: #f5f5f5;
}
header {
  background: #FFD700; /* AMARELO */
  color: #FF0000; /* VERMELHO */
  text-align: center;
  padding: 15px;
}
header h1 { margin: 0; font-size: 28px; font-weight: bold; }
header p { margin: 5px 0 0; color: #FF0000; }
#grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
  gap: 20px;
  padding: 20px;
  max-width: 1200px;
  margin: auto;
}
.card {
  background: white;
  border-radius: 10px;
  padding: 15px;
  box-shadow: 0 2px 5px rgba(0,0,0,0.1);
  text-align: center;
}
.card img {
  width: 100%;
  height: 200px;
  object-fit: contain;
  border-radius: 8px;
}
.card h3 {
  font-size: 16px;
  margin: 10px 0;
  color: #333;
}
.preco {
  font-size: 18px;
  font-weight: bold;
  color: #FF7000;
  margin: 5px 0;
}
.card a {
  display: inline-block;
  background: #25D366;
  color: white;
  padding: 8px 12px;
  border-radius: 5px;
  text-decoration: none;
  margin-top: 10px;
}

.zoom-img {
  width: 100%;
  height: 200px;
  object-fit: cover;
  cursor: pointer;
}

.card {
  overflow: hidden;
}
.modal {
  display: none;
  position: fixed;
  z-index: 1000;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0,0,0,0.9);
  text-align: center;
  padding-top: 50px;
}
.modal-conteudo { 
  max-width: 80%; 
  max-height: 80%; 
}
.fechar {
  position: absolute;
  top: 20px;
  right: 35px;
  color: white;
  font-size: 40px;
  font-weight: bold;
  cursor: pointer;
}
.fechar:hover { color: #bbb; }
#legenda { 
  color: white; 
  font-size: 20px; 
  margin-top: 15px; 
}
/* LIGHTBOX NOVO - Foto grande ao clicar */
#lightbox {
  display: none;
  position: fixed;
  z-index: 9999;
  left: 0;
  top: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0,0,0,0.95);
  padding-top: 50px;
}

.lightbox-conteudo {
  margin: auto;
  display: block;
  width: 100%;
  max-width: 95%;
  max-height: 85vh;
}

/* Deixa o cursor de mãozinha nas fotos dos produtos */
.card img {
  cursor: pointer;
}
